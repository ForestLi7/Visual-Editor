# Visual Editor — node2 本机 Docker 部署（容器 127.0.0.1:5174）
# 用法：将下面 map 放入 nginx.conf 的 http { } 内；
#       将 location 放入你的 server { } 内，然后 nginx -t && systemctl reload nginx

# ---------- http { } 内（全局只需一份）----------
# map $http_upgrade $connection_upgrade {
#     default upgrade;
#     ''      close;
# }

# ---------- server { } 内（HTTPS 测试域名示例）----------
# location /visual-editor/ {
#     proxy_pass http://127.0.0.1:5174/visual-editor/;
#     proxy_http_version 1.1;
#
#     proxy_set_header Host $host;
#     proxy_set_header X-Forwarded-Host $host;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection $connection_upgrade;
#
#     client_max_body_size 32m;
#     proxy_read_timeout 120s;
#     proxy_send_timeout 120s;
#     proxy_connect_timeout 60s;
# }

# 访问: https://{你的域名}/visual-editor/
# 自检: curl -s https://{你的域名}/visual-editor/api/health
